.vendor_mine
  .vendor_info
    .vendor_profile
    .vendor_info
      = current_vendor.phone
  .vendor_action_list
    .vendor_action_item
    .vendor_action_item
    .vendor_action_item

  .vendor_logout
    = form_tag vendor_logout_path, :method => "delete" do
      = submit_tag "退出登录"


