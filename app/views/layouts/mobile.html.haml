!!!
%html
  %head
    %title= @title || "萝卜停车"
    = stylesheet_link_tag    'mobile', media: 'all'
    = javascript_include_tag "http://webapi.amap.com/maps?v=1.3&key=#{Settings.amap_key}" 
    = csrf_meta_tags
    %meta{:name => "keywords",    :content => Settings.keywords}
    %meta{:name => "description", :content => Settings.description}
    %meta{:content => "width=device-width, initial-scale=1, maximum-scale=1.0", :name => "viewport"}
  %body
    = yield
    #tabs
      %a{:href => "/"}
        %span{:class => map_nav_class("map")} 附近
      %a{:href => "/mobile/hot_place"}
        %span{:class => map_nav_class("search")} 搜索
      %a{:href => "/mobile/setting"}
        %span{:class => map_nav_class("mine")} 我的
